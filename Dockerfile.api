# 使用 Node.js 官方镜像
FROM node:20-alpine

# 设置工作目录
WORKDIR /app

# 复制 package.json 和 package-lock.json
COPY package*.json ./

# 安装依赖
RUN npm ci --only=production

# 复制源代码
COPY server.js ./

# 暴露端口
EXPOSE 4000

# 启动命令
CMD ["node", "server.js"]
